// trigger.js — Wrapper fuer alle Adapter-Trigger (startCleaning, startCustomRoomCleaning,
// stopCleaning, chargeHome, Reset-Trigger fuer Verschleissteile). Einziger Sende-Weg, das
// Widget schreibt keine remote.*-States mehr direkt (WIDGET_ARCHITEKTUR.md Abschnitt 8.4/13).
// Wird gefuellt in Etappe B, Commit B3 (siehe WIDGET_UMBAU_PLAN.md Abschnitt 3).
