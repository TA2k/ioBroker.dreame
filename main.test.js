'use strict';

const { expect } = require('chai');

describe('Dreame Adapter — basic tests', () => {
  it('expect syntax works', () => {
    const result = 5;
    expect(result).to.equal(5);
  });
});
